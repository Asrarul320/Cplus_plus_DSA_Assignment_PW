// Q3 Calculate the time complexity for the following code snippet where k is some constant (k<<n).
// int c = 0;
// for(int i = 0; i < n; i += k) {
// c++;
// }


//ANS= The time complexity of the given code snippet is O(n), as the number of iterations depends on n and k is treated as a constant.